server:
  port: 8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  instance:
    hostname: localhost
spring:
  application:
    name: eureka-server
management:
  tracing:
    sampling:
      probability: 1.0